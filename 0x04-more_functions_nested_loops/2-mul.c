#include "main.h"

/**
 * mul - multiplies two integers
 * @a: firt multipler
 * @b: second multipler
 * Return: multiplication result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
