#include "main.h"

/**
 * _isupper - check for upper character
 * @c : check to character case
 * Return: 1 for upper 0 for any else
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}

